<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RoomBookingAndroidApp"
        tools:targetApi="31">
        <activity
            android:name=".rating.RateARoomActivity"
            android:exported="false"/>
        <activity
            android:name=".insertion.InsertRoomActivity"
            android:exported="false"/>
        <activity
            android:name=".addDates.AddDatesActivity"
            android:exported="false"/>
        <activity
            android:name=".managerConnection.ManagerConnectionActivity"
            android:exported="false"/>
        <activity
            android:name=".customerConnection.CustomerConnectionActivity"
            android:exported="false"/>
        <activity
            android:name=".homepage.HomepageActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
